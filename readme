# KF_QN_CNN_ICML2021

## Specification of dependencies

Python 3.7.6;
GCC 7.3.0;
cuda 11.0.3

torch 1.8.1,
torchvision 0.9.1,
numpy 1.18.0,
scipy 1.4.1,
pytz 2019.3,
psutil 5.6.7


## How to get results

See Demo.ipynb for results and command to produce the results.

### To tune the hyper-paramters:

For KF-BFGS-CNN, KF-BFGS(L)-CNN, and KFAC, change the arguments "lr" and "lambda_damping" in train_model();
For Adam, change the arguments "lr" and "Adam_epsilon";
For SGD-momentum, change the arguments "lr".

